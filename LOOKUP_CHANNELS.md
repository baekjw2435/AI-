# 사전 조회·탐색 채널

| 사전 | 조회 채널 | 명령 |
|---|---|---|
| 표준 | `1544553748565729381` | `!공격`, `!한방`, `!장문`, `!장문종결`, `!종결`, `!중간`, `!루트`, `!탐색` |
| 복합 | `1523328035686846495` | `!공격`, `!한방`, `!장문`, `!장문종결`, `!종결`, `!중간` |

조회 채널의 사전은 고정되어 있다. `!루트`와 `!탐색`은 표준 자료 전용이며 복합 채널에서 사용하면 표준 탐색 채널을 안내한다. 다른 채널에서 조회 명령을 사용하면 결과를 계산·출력하지 않고 전용 채널을 안내한다. 순위전 조회 지연보다 채널 제한을 먼저 적용한다. `!탐색`의 버튼도 표준 탐색 채널에서만 작동한다.

훈련실·경기장은 경기 시작·진행·기권 기능을 유지한다. 조회 명령의 인수 없는 사용, 공백 없이 붙인 사용, `!중간`의 페이지 조회에도 같은 채널 제한을 적용한다. 도움말, 사전 모드 확인, 관리자 경기 중단·지연 설정과 별도 게임·화학 기능은 기존 용도를 유지한다. 탐색 채널에서 경기 시작을 막는 기존 규칙도 유지한다.

`GUILD_ID`의 서버 제한은 그대로 적용한다. 과거 단일 채널용 `CHANNEL_ID`를 설정했어도 위 두 채널의 사전 조회 명령은 명시적인 전용 채널 설정을 따른다. 그 외 명령에는 기존 `CHANNEL_ID` 제한을 적용한다. 화학 채널은 기존 전용 분기를 사용한다.

설정과 명령 목록은 `main.py`의 `LOOKUP_CHANNELS`, `STANDARD_LOOKUP_COMMANDS`, `DICTIONARY_LOOKUP_COMMANDS`에 있다. 조회 명령의 허용 목록은 순위전 지연 설정과 별도로 적용하므로 지연을 꺼도 채널 제한이 해제되지 않는다.
