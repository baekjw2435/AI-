# 표준사전 중간말잇기 루트음절 연결수

표준사전 채널에서 기존 `!중간 <시작 음절>` 명령으로 한방·공격과 함께 **루트음절 연결수**를 볼 수 있다. 기존 공격·한방 데이터와 분류를 유지하며, 같은 단어가 연결수 조건에도 맞으면 양쪽에 표시한다. 복합사전 출력은 바꾸지 않는다.

## 선정 기준

`standard_words.txt` 전체에서 두 글자 이상인 단어의 **끝에서 두 번째 음절**을 확인한다. 사용자가 지정한 아래 음절에 해당하면 연결수로 표시한다. 일반 끝말잇기 루트 학습 점수나 중간 공격 목록 포함 여부로 제한하지 않는다.

```text
덕 슴 벽 돔 짝 킨 템 냐 럭 칫 죄 죽 업 융 엿 둑 듬 득 섯
율 짚 땀 핍 뱀 볕 냥 런 솥 족 숲 럼 름 늠 률 값
```

중복으로 전달된 `짝`, `뱀`은 한 번만 넣고, `름(늠)`은 두 음절을 모두 포함했다. 시작 음절 조회와 다음 연결 음절 표시에 봇의 기존 **표준 두음법칙** 함수를 그대로 사용한다. `름 → 늠`, `률 → 율`은 적용하며 역방향이나 비표준 변환은 추가하지 않는다. 예를 들어 기존 표준 매핑에서 `럼 → 넘`, `런 → 넌`, `럭 → 넉`, `냐 → 야`, `냥 → 양`은 적용하지 않는다.

이 연결수는 **중간말잇기가 1턴 남은 상황**에서 상대에게 넘길 음절을 찾는 용도다. 다음 차례의 중간말잇기가 계속된다고 가정해 공격 깊이를 계산하는 기능이 아니며, 남은 턴이나 실전 사용 단어를 자동으로 추적하지 않는다.

## 명령과 표시 예

| 명령 | 연결수 예 | 넘기는 음절 |
|---|---|---|
| `!중간 간` | 간**핍**히 | 핍 |
| `!중간 음` | 음**덕**가 | 덕 |
| `!중간 낭` | 낭**족**산 | 족 |
| `!중간 결` | 결**벽**성, 결**벽**증, 결안**죄**인, 결**핍**증 | 벽, 죄, 핍 |
| `!중간 반` | 반달가**슴**곰 | 슴 |
| `!중간 격` | 격정범**죄**인 | 죄 |
| `!중간 옥` | 옥**돔**과 | 돔 |

각 단어에서 연결 음절을 굵게 표시하며, `기**름**샘 → 름/늠`처럼 가능한 정방향 두음도 함께 보여 준다. 결과가 많으면 생략하지 않고 페이지로 나누며 `!중간 벽 2`처럼 다음 페이지를 조회한다. 페이지를 바꿔도 기존 한방·공격 필드는 유지한다. 기존 순위전 시간대 조회 지연과 채널별 사전 설정도 그대로 적용한다.

한방·공격 파일이 없어도 표준 단어 파일을 읽었다면 연결수는 표시하며, 빠진 자료는 안내한다. 연결수는 표준 단어를 읽을 때 색인을 만들어 조회하므로 루트 학습 데이터 파일이나 외부 API가 필요하지 않다.

## 확인

```bash
python -m unittest discover -s tests -v
python -m py_compile main.py
```

사용자가 제시한 7개 예시, 표준 두음 방향, 표준사전 전체 검색, 기존 공격 보존, 복합사전 분리, 모든 연결수의 페이지 접근 및 Discord 출력 한도, 실제 명령 분기를 확인한다. 테스트에서는 Discord 연결과 메시지 전송을 모의 처리한다.

표준 두음 매핑 참고: [국립국어원 한국어 어문 규범](https://www.korean.go.kr/kornorms/m/m_regltn.do)의 한글 맞춤법 제10~12항. 게임에서는 단어 첫머리 연결에 해당하는 음절 변환을 기존 규칙대로 적용한다.
