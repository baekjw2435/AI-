# 화학 채널 계산기

화학 채널 기본값: `1552593568357548032`.

기존 `discord.Client`와 `!명령어` 방식에 연결한 계산기다. 외부 AI·검색 API를 호출하지 않으며 계산 부분은 Python 표준 라이브러리만 사용한다. 기존 의존성 외에 추가 설치할 패키지는 없다. Python 3.10 이상을 사용한다.

## 명령어

| 명령 | 기능 |
|---|---|
| `!화학` | 사용법과 예시 |
| `!원자 Ca(OH)2` | 화학식 단위의 원자 수·전하·몰질량 |
| `!원자 2H2O + CO2` | 여러 화학식의 계수를 적용한 원자 수 합산 |
| `!질량 MgSO4·7H2O` | 평균 원자량에 따른 근사 몰질량 |
| `!계수 Mg(OH)2 -> Mg^2+ + OH^-` | 반응물과 생성물이 주어진 식의 최소 정수 계수 |
| `!양적 Mg(OH)2 -> Mg^2+ + OH^- \| Mg(OH)2=1mol` | 지정한 반응의 이론적 최대 생성량과 반응물 잔류량 |
| `!반응 C6H12O6 + 6O2 + 6H2O` | 완전 산화를 가정한 생성물 후보와 균형 총괄식 |
| `!반응 CaCl2 + Na2CO3 + KCl` | 여러 물질에서 생성물 후보·순이온식·분자식 예 찾기 |
| `!생성물 HCl + NaHCO3` | `!반응`과 같은 명령 |
| `!반응 HCl + NaOH \| 용매=물 \| 온도=25` | 조건을 확인한 등록 수용액 규칙 적용 |
| `!침전 CaCO3 \| 용매=물 \| 온도=25 \| Ca^2+=0.001M \| CO3^2-=0.001M` | 지정한 자유 이온 농도에서 이 고체의 포화 여부 |
| `!화학자료` | 데이터 출처와 현재 지원 범위 |

실제로 입력할 때 `|` 앞의 표 표시용 역슬래시는 쓰지 않는다. `!화학 원자 H2O` 같은 형태도 가능하다. `!화학 H2O`는 조성 계산, `!화학 A -> B`는 계수 계산, `!화학 A + B`는 반응 규칙 확인으로 연결된다. 슬래시 명령 등록은 필요 없다.

예를 들어 `!원자 2H2O + CO2`는 H 4개, C 1개, O 4개를 합산한다. 이 합계를 하나의 생성물 화학식으로 출력하지 않는다.

## 입력과 정확도

- 118개 원소 기호, 중첩 괄호 `()[]{}`, 결정수 `·`, 계수, 이온 전하, 상태 표시 `(s)/(l)/(g)/(aq)`, 전자 `e^-`를 해석한다.
- `SO₄²⁻`도 지원한다. `Fe3+`는 모호하므로 `Fe^3+`로 입력한다. 동위원소 `[13C]`, 변수 반복 단위 `(C2H4)n`, 비정수 조성은 지원하지 않는다.
- 원자 수는 정수, 계수는 유리수 연산으로 구하고 원소와 전하 보존을 다시 확인한다. 주어진 방향에서 모든 계수가 양수인 단일 비율만 확정한다. 여러 독립 반응이면 추가 조건을 요청한다.
- 기존에 적힌 계수는 재계산한다. 양적 계산에는 실제 양을 별도로 입력한다. `mol/mmol/g/mg`를 지원하며 모든 반응물의 양이 필요하다.
- 평균 원자량은 CIAAW 2024 축약 표에 따른 근삿값이다. 표준 원자량이 없는 원소에는 임의의 질량수를 넣지 않는다. 이온의 전자 질량 차이는 이 교육용 평균 몰질량에 별도로 보정하지 않는다.
- 문법이 맞는 화학식이라고 실제 존재하는 물질로 판정하지 않는다. 균형이 맞는 식이라고 실제 반응한다고 판정하지 않는다. 양적 계산은 주어진 단일 반응의 이론적 최대치이며 실제 수율이 아니다.

## 생성물 탐색 모델 (products-2.0.0)

`!반응`과 `!생성물`은 서로 다른 물질 2~16개에서 등록된 규칙에 맞는 생성물 후보를 찾는다. 물질이 세 개 이상이거나 일부 물질을 모른다는 이유로 탐색 전체를 중단하지 않는다. 후보마다 생성물, 원소·전하 보존을 확인한 최소 정수 계수, 가정, 추가로 필요한 조건을 표시한다. 일부만 해석하면 미판정 입력도 표시한다.

| 규칙 | 구현 범위 | 결과의 의미 |
|---|---|---|
| 완전 산화 | C·H 또는 C·H·O로 구성된 중성 조성 + O2 | 완전 산화하면 CO2·H2O가 된다는 가정의 총괄식. 실제 진행·구조·반응 경로를 예측하지 않음 |
| 산염기 | 강산·강염기 중화, 탄산·탄산수소·인산 종의 8개 규칙 | 단계별 또는 총괄 생성물 후보. 최종 종의 비율·pH·반응량은 별도 |
| 침전 | 6개 고체의 이온 조합 | 후보를 찾은 뒤 자유 이온 농도가 있으면 개별 Q/Ksp 계산 |
| 여러 후보 | 전체 입력의 이온 집합 및 각 산화 대상 탐색 | 후보를 모두 표시. 전부 완결됐다고 합산하거나 단일 최종 혼합물로 단정하지 않음 |

생성물 찾기는 원자 수 합산과 다르다. 수용액은 `chemistry_data/aqueous.json`의 21개 해리 기록 및 명시적 이온을 사용하고, `reaction_rules.json`의 반응 규칙을 적용한다. 물질 쌍의 답안을 하드코딩하지 않는다. 분자식 예는 등록된 두 물질에서 구성한 뒤 해리·구경꾼 이온 소거 결과가 원래 순이온식과 비례하는지 검증한 경우만 표시한다. 혼합물이 여러 성분이면 이 분자식 예는 해당 두 물질에 대한 부분 반응이다.

예를 들어 다음 입력은:

```text
!반응 C6H12O6 + 6O2 + 6H2O
```

아래 핵심 결과와 가정 설명을 포함한다:

```text
가정에 따른 생성물 후보
후보 1 · C6H12O6 완전 산화 가정
생성물 후보: CO2 + H2O
C6H12O6 + 6O2 → 6CO2 + 6H2O
```

입력한 물은 이미 존재한 물로 별도 표시하고 새 생성량에 더하지 않는다. 앞의 계수는 후보 탐색에서 실제 투입량으로 사용하지 않았음을 표시한다. 생성량을 원하면 선택한 반응식과 실제 양을 `!양적`에 지정한다. 중복 화학종은 한 번만 탐색한다. 동일 분자식으로 포도당과 다른 이성질체를 구분할 수 없으므로 물질 이름·구조를 확정하지 않는다.

후보 탐색 자체에는 온도·용매가 없어도 된다. 이때 **수용액 후보는 물·25 °C·희석 용액이라는 적용 가정과 미확인 조건을 표시**하며 실제로 그 조건이었다고 처리하지 않는다. `| 용매=물 | 온도=25`를 지정하면 그 범위의 판정을 진행한다. 다른 온도·용매, 촉매·압력·pH·시간 조건에 대한 계산은 미지원으로 표시한다. 상이 명시된 고체·기체를 수용액의 해리 물질로 바꾸지 않는다. 용해 가능한 기록이라고 무조건 모두 녹는다는 양적 계산도 하지 않는다.

`| 유형=수용액` 또는 `| 유형=완전산화`로 규칙 종류를 제한할 수 있다(기본 `자동`). 완전 산화 총괄식은 반응 조건을 정하는 도구가 아니므로 온도·용매·자유 이온 농도·고체 유무 옵션과 함께 사용할 수 없다. 산소가 없거나 전하가 있는 조성, C/H/O 외 원소, 결정수 표기, 양의 산소 소비 계수를 갖지 않는 조성에는 이 규칙을 적용하지 않는다. 조성에 맞는다는 이유로 실제 존재하는 물질이라고 판정하지 않는다.

모든 유기 반응·산화환원·분해·합성 반응과 연쇄 반응의 최종 상태를 다루는 범용 예측기는 아니다. 구조 식별과 자료가 필요한 물질은 미판정으로 남기며, 일부 규칙이 맞더라도 다른 경로가 없다고 주장하지 않는다. 자료가 없다는 이유로 '반응하지 않음'을 출력하지 않는다.

## 수용액 포화 판정

포화 판정용 고체는 다음 6개이다.

| 고체 | 25 °C에서 사용하는 표의 Ksp |
|---|---:|
| CaCO3 | 8.7e-9 |
| Ca(OH)2 | 1.3e-6 |
| Mg(OH)2 | 8.9e-12 |
| CaSO4·2H2O | 6.1e-5 |
| Ca3(PO4)2 | 1.3e-32 |
| CaHPO4 | 7e-7 |

수치는 아래 원 교재의 Appendix J를 기준으로 한다. 서로 다른 데이터베이스의 값을 혼합하지 않았으며, 결정형별 열역학 데이터베이스 전체를 구현한 것은 아니다. 고체를 바꾸거나 데이터를 확대할 때는 상과 상수 정의까지 검토해야 한다.

`!침전`의 농도는 **최종 용액의 자유 이온 농도**이다. 원액 농도나 원소의 총농도가 아니다. 탄산·인산의 산염기 평형, 착물 형성 등으로 자유 이온 농도가 달라지는 문제는 이 버전이 자동으로 풀지 않는다. 사용자가 그 효과를 고려한 자유 이온 농도를 제공한 경우에만 계산한다.

희석 용액에서 자유 이온 농도로 활동도를 근사하고, 물의 활동도는 1로 둔다. 이 근사를 제한하려고 입력 자유 이온 농도를 각각 0.01 M 이하로 한정했다. 이 한도는 앱의 보수적인 입력 제한이며, 그 이하의 모든 용액에서 비이상성이 사라진다는 뜻은 아니다.

Q가 Ksp보다 크면 과포화, 작으면 불포화로 구분한다. 비율의 상대 차이가 1e-6 이내이면 수치 모델상 경계로 분류한다. 이 수치 허용오차를 실험 상수의 불확실성으로 해석하지 않는다. `| 고체=있음`이면 불포화일 때 용해 방향을 함께 표시한다. 실제 변화량·속도·결정 생성 시간은 출력하지 않는다.

여러 고체 후보가 있으면 같은 최종 용액의 자유 이온 농도를 각 후보의 식에 나누어 적용한다. 개별 과포화 판정이 전체 평형을 함께 푼 결과는 아니다. 후보에 사용되지 않는 농도 옵션은 거부한다. `고체` 옵션은 후보가 하나일 때만 받으며, 여러 후보에서는 `!침전 고체화학식`으로 고체를 지정한다.

## 봇 연결과 배포

`main.py`가 화학 채널을 먼저 분기하고 기존 게임·사전 명령은 기존 처리로 이어진다. `GUILD_ID` 제한은 화학 채널에도 유지한다. 기존 `CHANNEL_ID`는 기존 기능에만 적용하므로 다른 채널 ID로 설정되어 있어도 화학 채널을 따로 사용할 수 있다.

채널을 바꾸려면 `CHEMISTRY_CHANNEL_ID` 환경변수를 설정한다. 기존 봇 토큰과 Message Content Intent 설정은 그대로 사용한다. 봇 계정에 해당 채널의 보기·메시지 보내기·링크 첨부(Embed Links) 권한이 있어야 한다.

배포에는 `main.py`, `chemistry_bot.py`, `chemistry_engine.py`, `chemistry_reactions.py`, `chemistry_data/`가 함께 필요하다. `main.py` 하나만 교체하면 새 기능이 로드되지 않는다. 기존 `Procfile`의 시작 명령은 그대로 사용할 수 있다.

입력 길이·괄호 깊이·물질 수·정수 크기를 제한한다. 계산은 이벤트 루프 밖에서 실행하고 사용자별 짧은 쿨다운과 동시 요청 상한을 둔다. 출력의 멘션은 비활성화한다. 입력을 실행 코드로 평가하지 않는다.

## 테스트

```bash
python -m unittest discover -s tests -v
python -m py_compile main.py chemistry_engine.py chemistry_reactions.py chemistry_bot.py
```

총 63개 테스트로 원자 수, 결정수, 전하, 전자, 계수의 유일성, 원자량 누락, 단위 변환, 제한 반응물, 조건 누락, 포화 경계, Discord 응답 길이, 채널·서버 필터를 검사한다. 생성물 탐색은 사용자 입력의 회귀 사례, 여러 물질·경쟁 후보·모르는 첨가물·명시적 상·중복 입력·유형별 조건·보존 법칙을 검사한다. 등록된 해리 물질의 모든 두 물질 조합에서 생성한 반응식과 분자식 예의 보존도 확인한다. Discord 메시지 전송은 모의 객체를 사용하며 실제 채널에 테스트 메시지를 보내지 않는다.

## 자료 출처

- [CIAAW — Abridged Standard Atomic Weights 2024](https://www.ciaaw.org/abridged-atomic-weights.htm)
- [OpenStax Chemistry 2e — 4.2 Classifying Chemical Reactions](https://openstax.org/books/chemistry-2e/pages/4-2-classifying-chemical-reactions)
- [OpenStax Chemistry 2e — 15.1 Precipitation and Dissolution](https://openstax.org/books/chemistry-2e/pages/15-1-precipitation-and-dissolution)
- [OpenStax Chemistry 2e — 14.1 Brønsted-Lowry Acids and Bases](https://openstax.org/books/chemistry-2e/pages/14-1-bronsted-lowry-acids-and-bases)
- [OpenStax Chemistry 2e — 14.5 Polyprotic Acids](https://openstax.org/books/chemistry-2e/pages/14-5-polyprotic-acids)
- [OpenStax Chemistry 2e — Appendix J: Solubility Products](https://openstax.org/books/chemistry-2e/pages/j-solubility-products)
- [discord.py API](https://discordpy.readthedocs.io/en/stable/api.html)

원 교재: Paul Flowers, Klaus Theopold, Richard Langley, William R. Robinson, Chemistry 2e, OpenStax (2019). 수치와 화학식 등 사실 자료만 필요한 범위에서 기록했으며 교재의 문장·그림을 복제하지 않았다. 계산 코드와 한국어 설명은 이 기능을 위해 작성했다. 자료 확인일: 2026-09-24.
